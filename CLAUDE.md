# Working in this repo

## Git / PR workflow

Once a branch is pushed and its pull request is open, merge it to `main`
automatically (squash merge, matching this repo's existing history) — don't
wait for an explicit "merge" instruction each time. Still hold off and flag
it instead if something is actually wrong: a real CI failure, an unresolved
review comment, or a merge conflict.
